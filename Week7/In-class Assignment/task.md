# Lecture assignment 
Use the following code as a base to add the additional functions:

![Calculator]https://github.com/ADirin/week7_calculator_fx_db.git

## Implement the Following Additional Functions Based on the Given Calculator Repository

  1. Subreact

  2. Division
  
After implementing the functions, run the Jenkins pipeline:

  - Run the Docker image and make sure the database and the table are created successfully.
  - The given code is based on MariaDB; you may change it if necessary based on your own preferences.
  - Furthermore, the port used in docker-compose may need to be changed based on your own MariaDB setup

## Submission 
  - ake a screenshot of the Jenkins stages performance (only the graphics, not the console output).
  - Take a screenshot of the Docker image UI performance and the sample database table results.
  - **Submit** your repository link along with the screenshots (points 1 and 2) on OMA.
